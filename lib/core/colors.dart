import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.green;
  static const primaryDark = Color(0xFF006400);
  static const positiveProfit = Color(0xFF2E7D32);
  static const negativeProfit = Color(0xFFC62828);
  static const cardBackground = Colors.white;
  static const background = Color(0xFFF5F5F5);
  static const border = Colors.grey;
}
